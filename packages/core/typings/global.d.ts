
export {};

declare global {
    //eslint-disable-next-line no-var
    var _isSystemWin: boolean;
    //eslint-disable-next-line no-var
    var _doResolve;
    //eslint-disable-next-line no-var
    var _getConfigProp
    //eslint-disable-next-line no-var
    var fetch;
    //eslint-disable-next-line no-var
    var RNV_CONTEXT;
    //eslint-disable-next-line no-var
    var RNV_API;
}
